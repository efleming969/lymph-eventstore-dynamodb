{
  "name": "lymph-eventstore-dynamodb",
  "version": "0.10.0",
  "description": "DynamoDB event storage",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "aws-sdk": "^2.45.0",
    "jest": "^20.0.3"
  },
  "jest": {
    "testMatch": [
      "**/*.tests.js"
    ]
  },
  "scripts": {
    "coverage": "jest --coverage",
    "test": "jest"
  }
}
